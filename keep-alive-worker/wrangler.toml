# Cloudflare Worker - 保持 Render 后端活跃
# v1.0 - 每 5 分钟 ping 后端健康检查端点

name = "smartice-keep-alive"
main = "index.js"
compatibility_date = "2024-01-01"

# 定时触发器 - 每 3 分钟执行一次 (更频繁以防 Render 睡眠)
[triggers]
crons = ["*/3 * * * *"]
